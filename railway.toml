[build]
builder = "RAILPACK"

[deploy]
startCommand = "bun run start & bun run backfill:settlements"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
