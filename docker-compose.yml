services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ewms
      MYSQL_USER: ewms
      MYSQL_PASSWORD: ewms
    ports:
      - "3306:3306"
    volumes:
      - ewms_mysql:/var/lib/mysql

volumes:
  ewms_mysql:
